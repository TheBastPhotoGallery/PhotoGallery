<!DOCTYPE html>
<html lang="en">
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<link href="/style.css" rel="stylesheet">

<head>

    <title>dashboard</title>

</head>
<body>

<div class="container">
    <div class="log">
        <button class="in"><a class="start" href="/">Start</a></button>
    </div>
    <script>
        let picker = document.getElementById('picker');
        let listing = document.getElementById('listing');

        picker.addEventListener('change', e => {
            for (let file of Array.from(e.target.files)) {
                let item = document.createElement('li');
                item.textContent = file.webkitRelativePath;
                listing.appendChild(item);
            };
        });
    </script>
    <div>
        <a href="/myalbums"><h2>My folders</h2></a>
        <h2>Upload folder</h2>
        <div class="picker"><input type="file" id="picker" name="fileList" webkitdirectory multiple></div>

        <ul id="listing"></ul>
        <input class="in" type="submit" value="Submit">


    </div>
</div>
</body>
</html>